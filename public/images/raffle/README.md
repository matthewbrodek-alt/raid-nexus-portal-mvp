Put the iOS giveaway character fallback image here:

```text
macheha-ios.png
```

The page `/raffle` uses it only on iPhone/iPad, because iOS WebKit can display WebM alpha incorrectly:

```text
/images/raffle/macheha-ios.png
```
