Place the heading font files here if you have the licensed font:

- `right-grotesk-light.woff2`
- `right-grotesk-light.woff`

The site already references `Right Grotesk Light`. If the files are absent, browsers will fall back to Inter/system fonts without breaking the layout.
